<% provide(:title, "Forgot password") %>
<h1>Forgot password</h1>

<div>
  <div>
    <%= form_with(url: new_password_reset_path, scorp: :password_reset) do |f| %>
      <%= f.label :email %>
      <%= f.email_field :email %>

      <%= f. submit "Submit" %>
    <% end %>
  </div>
</div>