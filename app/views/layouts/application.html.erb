<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Delivery App" %></title>

    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/a8a441addb.js" crossorigin="anonymous"></script>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    >

    <!-- Google Maps -->
    <% unless content_for?(:google_maps_loaded) %>
      <% content_for :google_maps_loaded, true %>

      <script
        src="https://maps.googleapis.com/maps/api/js?key=<%= Rails.application.credentials.google_maps_api_key %>&loading=async"
        async
        defer>
      </script>
    <% end %>

    <!-- Sortable -->
    <script
      src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.0/Sortable.min.js"
      defer>
    </script>

  </head>

  <body class="<%= controller_name %>-<%= action_name %>">
    <header>
      <%= render "layouts/header" %>
    </header>

    <div class="wrapper">
      
      <aside class="sidebar" id="sidebar">

        <% if content_for?(:sidebar_pc) %>
          <div class="sidebar-extra pc-only">
            <%= yield :sidebar_pc %>
          </div>
        <% end %>

        <%= render "layouts/sidebar" %>

      </aside>


      <main class="main">
        <div class="container-fluid">

          <% flash.each do |type, message| %>
            <div class="alert alert-<%= type %>">
              <%= message %>
            </div>
          <% end %>

          <%= yield %>

          <% if content_for?(:sidebar_mobile) %>
            <div class="mobile-extra">
              <%= yield :sidebar_mobile %>
            </div>
          <% end %>

        </div>
      </main>
    </div>

    <footer class="footer">
      <%= render "layouts/footer" %>
    </footer>
  </body>
</html>
