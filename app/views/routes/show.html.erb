<div class="route-show-map">
  <div id="map"></div>
</div>

<div class="route-info-card">
  <button onclick="goToGoogleMap()", class="btn-simple">
    GoogleMapで確認する
  </button>
  <%= link_to "編集する", edit_town_route_path(@town, @route), class:"btn-edit" %>
</div>

<div class="route-info-card">
  <p>制作者：<%= @route.user&.name || "不明" %></p>
  <p>コメント：<%= @route.description %></p>
</div>

<div class="route-info-card">
  <%= link_to "一覧へ戻る", town_path(@town), class:"btn-back" %>
</div>

<% content_for :sidebar do %>
  <%= render "route_info", editable: false %>
<% end %>


<!-- javascript関連 -->

<div id="route-data"
  data-points='<%= raw(
    @route.route_points.map { |p|
      {
        lat: p.latitude,
        lng: p.longitude,
        address: p.address
      }
    }.to_json
  ) %>'>
</div>


